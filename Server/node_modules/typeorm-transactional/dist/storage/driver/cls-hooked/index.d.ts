import { StorageKey, StorageValue, StorageDriver } from '../interface';
export declare class ClsHookedDriver implements StorageDriver {
    private context;
    constructor();
    get active(): boolean;
    private get store();
    get<T>(key: StorageKey): T;
    set(key: StorageKey, value: StorageValue): void;
    run<T>(cb: () => T): T;
}
