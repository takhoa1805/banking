import { DynamicModule, OnApplicationShutdown } from '@nestjs/common';
import { ModuleRef } from '@nestjs/core';
import { CassandraModuleAsyncOptions, CassandraModuleOptions, CassandraOptions } from './cassandra.interface';
export declare class CassandraModule implements OnApplicationShutdown {
    private readonly clientOptions;
    private readonly clientToken;
    private readonly moduleRef;
    constructor(clientOptions: CassandraOptions, clientToken: string, moduleRef: ModuleRef);
    static forRoot(options: CassandraModuleOptions): DynamicModule;
    static forRootAsync(options: CassandraModuleAsyncOptions): DynamicModule;
    onApplicationShutdown(): Promise<void>;
}
